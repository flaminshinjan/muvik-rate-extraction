/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class ChatModelAuthError extends Error {
    toString() {
        return `${this.name}: ${this.message}${this.cause ? ` (Caused by: ${this.cause})` : ''}`;
    }
    constructor(message, cause){
        super(message), _define_property(this, "cause", void 0), this.cause = cause;
        this.name = 'ChatModelAuthError';
        if (Error.captureStackTrace) Error.captureStackTrace(this, ChatModelAuthError);
    }
}
export { ChatModelAuthError };

//# sourceMappingURL=errors.mjs.map